@model IEnumerable<IvyBio.Models.Domain.Movie>

@{
    Layout = "_Layout";
}
<h1> Result</h1>
<section class="search-results mb-4">
    <div class="row">
        @foreach (var movie in Model)
        {
            @*hämta från Shared /_MoiveCard*@
            <partial name="_MovieCard" model="movie" />

        }
    </div>
</section>